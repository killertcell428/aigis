# Cycle Changes — 2026-05-16T00-00

## Domain
evasion-obfuscation (#7)

## Cycle Index
7

## Summary

**Researched:** Third evasion-obfuscation cycle. Surveyed new obfuscation families not yet
covered: Mathematical Unicode alphanumeric symbols (U+1D400–U+1D7FF), zalgo/combining
diacritics (U+0300–U+036F), zero-width keyword splitters, soft hyphen injection (U+00AD),
HTML entity encoding, and variation selector density attacks.

**Implemented:** Two new DetectionPattern entries in `ENCODING_BYPASS_PATTERNS`:
- `enc_math_unicode_keywords` (score 45) — Mathematical/styled Unicode letter obfuscation
- `enc_zalgo_text` (score 35) — Zalgo / excessive combining diacritic attack

**What changed for users:** aigis now detects two additional invisible-character obfuscation
classes. Agents that receive prompts using Mathematical Alphanumeric Symbols (bold/italic/script/
fraktur/monospace Unicode letter variants) or Zalgo text (stacked combining diacritics) to bypass
ASCII keyword filters will now be flagged with audit-log findings.

**Deferred:** Four additional candidate hardenings held back for future cycles.

## Files Touched
- `aigis/filters/patterns.py` — added `enc_math_unicode_keywords` and `enc_zalgo_text` to
  `ENCODING_BYPASS_PATTERNS`
- `tests/test_evasion_obfuscation.py` — added `TestMathUnicodeKeywordsPattern` (9 tests) and
  `TestZalgoTextPattern` (7 tests)

## Quality Gate Results

### Formatter
`uv run ruff format aigis/ tests/` — 1 file reformatted (test file whitespace), 140 files left
unchanged.
`uv run ruff format --check aigis/ tests/` — 141 files already formatted.

### Lint
`uv run ruff check aigis/ tests/` — All checks passed.

### Tests
`uv run pytest --tb=no -q` — **16 failed, 1421 passed, 5 skipped** in 71.37s

Pre-existing failures (not caused by this cycle's changes):
- `tests/test_spec_lang.py` — 14 failures (YAML parser/DSL expectation mismatches; pre-existing)
- `tests/test_guard.py` — 2 failures (custom policy YAML failures; pre-existing)

New tests added this cycle: 16 new evasion tests all pass.

## Research File
`auto-improvement/research/2026-05-16T00-00_7-evasion-obfuscation.md`

## Implementation Caveats
- `enc_math_unicode_keywords`: threshold of 4+ chars avoids FPR from individual styled math
  symbols (e.g., 𝑥, 𝜙) used in isolated mathematical notation.
- `enc_zalgo_text`: threshold of 3+ consecutive combining marks avoids FPR from legitimate
  double-diacritic languages (Vietnamese, etc.).

## Pending Ideas Captured This Cycle
1. Zero-width keyword splitter detection (ZW chars interleaved in keywords)
2. Soft hyphen (U+00AD) density rule
3. HTML entity decode pass for web-agent pipelines
4. Variation selector density rule (U+FE00–FE0F, U+E0100–E01EF)

## Release Decision Input
Current `## [Unreleased]` now contains:
- 2 detectors from cycle 6 (multi-agent): `_CHAT_TEMPLATE_INJECTION_PATTERNS`,
  `_SAFETY_SPOOF_PATTERNS`
- 2 detectors from this cycle: `enc_math_unicode_keywords`, `enc_zalgo_text`

Total: 4 new detectors/pattern groups in Unreleased → meets the "3 or more" release threshold.
**Recommend: release v1.1.1.**
