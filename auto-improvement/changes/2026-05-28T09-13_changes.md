# Changes: cycle 3 (jailbreak-extraction) — 2026-05-28T09-13

## Domain: jailbreak-extraction (index 3, fourth pass)

## Summary

### Researched
Two distinct jailbreak attack families with clear regex-detectable setup markers and no prior
coverage in aigis:
1. **MetaBreak** (arXiv:2510.10271, Oct 2024) — special chat-template token injection
2. **CipherChat / SEAL** (arXiv:2308.06463 / arXiv:2505.16241) — cipher role-framing jailbreaks

Additional research covered Crescendo multi-turn escalation (pending), Sugar-Coated Poison
(pending), and operator impersonation patterns (pending — false-positive risk too high this
cycle).

### Implemented
Two new detection rules added to `JAILBREAK_ROLEPLAY_PATTERNS` in
`aigis/filters/patterns.py`:

**`jb_special_token_injection`** (score 80, input filter):
Matches chat-template special tokens (`<|im_start|>`, `<|im_end|>`, `[INST]`, `[/INST]`,
`<<SYS>>`, `<</SYS>>`, `<|eot_id|>`, `<|endoftext|>`, `<|system|>`, `<|user|>`,
`<|assistant|>`) in user-submitted text. These are exclusively inference-framework delimiters
with no legitimate use in user messages; any occurrence is a reliable indicator of an attempt
to hijack conversation structure.

**`jb_cipher_role_framing`** (score 65, input filter):
Three-arm pattern covering:
- Arm 1: "we will communicate entirely in ROT13/Base64/Caesar" (CipherChat role setup)
- Arm 2: "respond/answer only in [cipher]" (persona assignment form)
- Arm 3: "decode [cipher chain] then answer" (SEAL stacked cipher endpoint)

Closes a gap in `pi_encoding_bypass` which only matched `(cipher_name) + (instruction|command)`
and missed the role-assignment and decode-then-answer forms.

## User-visible impact

- User input containing chat-template special tokens is flagged at score 80 (HIGH) before
  reaching the model. This blocks MetaBreak-style conversation-structure hijack attempts with
  near-zero false-positive rate.
- Input instructing the model to communicate exclusively in ROT13/Base64/Caesar cipher is
  flagged at score 65 (MEDIUM-HIGH), closing the setup-phrase gap not covered by the existing
  encoding bypass rule.

## Files touched
- `aigis/filters/patterns.py` — +81 LOC (2 new DetectionPattern entries)
- `tests/test_jailbreak_patterns.py` — +53 LOC (pattern count updated 13→15; 2 new test
  classes with 6+7=13 test methods)

## Formatter result
`ruff format --check aigis/ tests/` → 148 files already formatted (no changes needed)

## Lint result
`ruff check aigis/ tests/` → All checks passed

## Test result
1639 passed, 19 failed, 5 skipped, 5 errors (measured via `uv run pytest --tb=no -q`)

The 19 failures and 5 errors are pre-existing and unrelated to this cycle's changes
(verified by `git stash` + rerun):
- `test_spec_lang.py` (11 failures) — spec_lang module tests, pre-existing
- `test_guard.py` (2 failures) — YAML policy loading, pre-existing
- `test_oss_comparison_bench.py` (3 failures) — HTTP adapter network tests, pre-existing
- `test_release_preflight.py` (5 errors) — release infrastructure, pre-existing

My 13 new tests all pass (`tests/test_jailbreak_patterns.py` → 66 passed, 0 failed).

## Research file used
`auto-improvement/research/2026-05-28T09-13_3-jailbreak-extraction.md`

## Pending ideas (this cycle)
- Crescendo final-turn pivot detector (multi-turn context required)
- Sugar-Coated Poison trigger pattern (false-positive tuning needed)
- Operator/developer impersonation pattern (false-positive risk in enterprise contexts)

## Release decision
[Unreleased] now has 2 new rules since v1.1.8. Release threshold is 3. No release this cycle.
