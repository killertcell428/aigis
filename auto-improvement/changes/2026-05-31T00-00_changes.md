# Changes: jailbreak-extraction (Cycle 3, fourth pass)

## Cycle info
- **Domain:** jailbreak-extraction (index 3)
- **Cycle UTC:** 2026-05-31T00-00
- **Research file:** [research/2026-05-31T00-00_3-jailbreak-extraction.md](../research/2026-05-31T00-00_3-jailbreak-extraction.md)

## Summary

### Researched
Fourth pass at jailbreak/system-prompt-extraction techniques. This pass focused on:
1. **MetaBreak** (arxiv:2510.10271, Oct 2025) — special-token injection to hijack chat-template role structure.
2. **Creative-format bypass** (arxiv:2511.15304, Nov 2025) — encoding harmful requests as poetry, rap, or song.
3. **JBFuzz** (arxiv:2503.08990, Mar 2025) — fuzzing-based jailbreak automation (99% ASR; no single detectable pattern).
4. **Deceptive Delight** (Unit 42, 2024) — multi-turn narrative co-occurrence attack (deferred: multi-turn only).
5. **Autonomous LLM-vs-LLM jailbreaks** (Nature Communications 2026) — confirms rule-surface-area reduction is the right defense.

### Implemented
Two new detection patterns added to `aigis/filters/patterns.py` (`JAILBREAK_ROLEPLAY_PATTERNS`):

1. **`jb_special_token_injection`** (score 70, input filter): Detects model chat-template special tokens (`<|im_start|>`, `<|im_end|>`, `<|begin_of_text|>`, `<|eot_id|>`, `<|endoftext|>`, `[INST]`, `[/INST]`, `<<SYS>>`, `<</SYS>>`) appearing in user-supplied text. These tokens are reserved control markers and should never appear in legitimate user input. Covers MetaBreak (arxiv:2510.10271) which boosted PAP ASR by +24.3% and GPTFuzzer by +20.2%.

2. **`jb_poetry_harmful_framing`** (score 60, input filter): Detects requests to express harmful content (drug synthesis, weapons, malware, violence) as a poem, rap, song, haiku, or similar creative form. Covers the adversarial poetry bypass technique (arxiv:2511.15304; 62% ASR across 25 frontier models).

### User-visible impact
- AI agents, web scrapers, and chatbot applications that pass user input through aigis will now detect attempts to inject model-level control tokens (a near-certain attack signal) and creative-format requests pairing a poetic directive with a specific harmful topic.
- Two new rules bring the total `JAILBREAK_ROLEPLAY_PATTERNS` count from 13 to 15.

### Not implemented / deferred
- **Deceptive Delight**: Multi-turn narrative co-occurrence; single-turn regex has unacceptable false-positive rate without session context. → pending.
- **JBFuzz**: Generates mutations of existing templates; no single new pattern, but broader rule coverage reduces its effective attack surface.

## Files touched
- `aigis/filters/patterns.py` — +73 LOC (two new DetectionPattern entries in JAILBREAK_ROLEPLAY_PATTERNS)
- `tests/test_jailbreak_patterns.py` — +61 LOC (two new test classes: TestSpecialTokenInjection × 8 tests, TestPoetryHarmfulFraming × 8 tests; updated pattern count from 13 to 15)

## Quality gate results
- **Formatter:** `ruff format` — 148 files unchanged
- **Format check:** `ruff format --check` — all files already formatted
- **Lint:** `ruff check` — all checks passed
- **Tests:** 1642 passed, 19 failed (pre-existing; same set as v1.1.8 cycle), 5 skipped, 5 errors (pre-existing)

## Implementation caveats
- The special-token pattern uses a plain alternation without DOTALL since these tokens are single-line markers. False positives are extremely unlikely: these token strings do not appear in natural English or typical code comments.
- The poetry pattern requires co-occurrence of a creative-format directive AND a harmful topic keyword within 200 chars to keep false positives low. Benign poetry requests (love poem, nature rap) are not flagged.

## Pending ideas this cycle: 1
- `auto-improvement/pending/2026-05-31_deceptive-delight-multi-turn.md`

## Release decision input
- 2 new detection patterns in this cycle.
- Current Unreleased section already contains LangGraph integration items (manual, not auto-improvement).
- Total unreleased auto-improvement detectors since v1.1.8: 2 (this cycle only).
- Release threshold is 3+ new detection rules OR a compliance template. Not yet met from auto-improvement perspective.
- **Decision: no release this cycle.** Wait for next cycle to accumulate more rules.
