# Changes: jailbreak-extraction — 2026-05-30T09-13

## Domain
jailbreak-extraction (index 3, fourth pass)

## Cycle index
3

## Summary

### Researched
Fresh material on creative-format jailbreaks (adversarial poetry, arxiv:2511.15304),
special token injection (MetaBreak, arxiv:2510.10271), fuzzing-based jailbreaking
(JBFuzz, arxiv:2503.08990, 2026), sockpuppeting API prefill exploitation (Trend Micro, 2026),
Crescendo multi-turn incremental jailbreaking (arxiv:2404.01833, USENIX Security 2025),
and RoleBreaker persona jailbreaks (arxiv:2507.22171, 2025).

### Implemented
Two new detection patterns added to `JAILBREAK_ROLEPLAY_PATTERNS` in
`aigis/filters/patterns.py`:

1. **`jb_poetry_harmful_framing`** (score 55, input filter) — Creative-format jailbreak:
   a poem, rap, ballad, song, or haiku directive combined with a harmful how-to request
   or dangerous subject keyword (methamphetamine, fentanyl, nerve agent, explosive, etc.)
   within approximately 200 characters. Addresses the pending item from the 2026-05-10
   research pass where LOC budget was exhausted.

2. **`jb_special_token_injection`** (score 70, input filter) — Detects injection of
   model-specific control tokens (`<|system|>`, `<|im_start|>`, `<|endoftext|>`,
   `[INST]`, `<<SYS>>` etc.) into user-supplied text. Near-zero false-positive rate since
   these tokens never appear in legitimate user input.

### Deferred (sent to pending)
- Time Bandit temporal framing jailbreak (fictional era + harmful request) — needs careful
  regex tuning to avoid false positives in legitimate historical questions.
- JBFuzz adaptive mutation coverage — requires behavioral/rate-limit monitoring layer.
- Crescendo multi-turn incremental jailbreaking — requires stateful session detection.

## User-visible impact
Two new jailbreak attack variants are now blocked in the input scanner:
- Requests that encode harmful instructions inside creative formats (poems, raps, songs)
  are flagged before reaching the model.
- Inputs containing LLM-internal control tokens are detected, alerting operators to
  attempts to hijack role boundaries.

## Files touched
- `aigis/filters/patterns.py` — 2 new DetectionPattern entries appended to JAILBREAK_ROLEPLAY_PATTERNS
- `tests/test_jailbreak_patterns.py` — 14 new test cases (7 for poetry, 7 for special tokens);
  pattern count assertion updated from 13 → 15

## Quality gate
- `ruff format`: 1 file reformatted (test file spacing), all clean after
- `ruff format --check`: 148 files already formatted (clean)
- `ruff check`: All checks passed
- `pytest`: 19 failed (pre-existing), 1640 passed, 5 skipped, 5 errors (pre-existing)
  - Pre-existing failures: test_spec_lang (15 tests), test_guard (2 tests),
    test_oss_comparison_bench (3 tests), test_release_preflight (5 errors)
  - None of these failures are related to this cycle's changes

## Research file used
`auto-improvement/research/2026-05-30T09-13_3-jailbreak-extraction.md`

## Implementation caveats
- `jb_poetry_harmful_framing` uses a 200-char wildcard window between the creative-format
  directive and the harmful keyword; longer prompts that bury the harmful subject beyond
  200 chars will not be caught. This is intentional — wider windows increase false positives
  on legitimate creative writing requests.
- `jb_special_token_injection` matches exact token strings; obfuscated variants (e.g., with
  zero-width characters inserted) are handled by the existing encoding/obfuscation patterns
  (`enc_zalgo_combining`, `enc_diacritics_keywords`).

## Pending ideas captured this cycle
1. `auto-improvement/pending/2026-05-30_time-bandit-temporal-jailbreak.md`

## Release decision input
- Unreleased section: 2 non-detection items (LangGraph integration docs)
  + 2 new detection rules this cycle = 2 detection rules total
- Release threshold: 3 or more new detection rules accumulated
- Decision: **no release** — threshold not met
