# Changes: jailbreak-extraction — 2026-05-24T00-00

## Domain: jailbreak-extraction
## Cycle index: 3
## Cycle timestamp: 2026-05-24T00-00

---

## Summary

**Researched:** Chat-template token injection (sockpuppetting / MetaBreak), payload splitting
(Speak Easy), PANDAS many-shot enhancement, and mitigating many-shot jailbreaks with a single
demonstration. Confirmed coverage gaps for token injection and step-enumerated decomposition;
confirmed existing patterns cover the PANDAS attack surface.

**Implemented:** Two new detection rules in `aigis/filters/patterns.py`:
1. `jb_chat_template_injection` — flags raw chat-model special tokens in user text
2. `jb_payload_splitting` — flags numbered-step decomposition with dangerous keyword in step 3+

**What changed for users:** AI agent inputs that embed raw model delimiters
(`<|im_start|>assistant`, `[/INST]`, `<<SYS>>`, `<|start_header_id|>assistant<|end_header_id|>`,
`<|assistant|>`, `<start_of_turn>model`) are now flagged with score 75. Numbered-step
decomposition with a dangerous topic keyword at step 3 or later is now flagged with score 45.

---

## Files Touched

- `aigis/filters/patterns.py` — +2 DetectionPattern entries in JAILBREAK_ROLEPLAY_PATTERNS (~52 LOC)
- `tests/test_filters.py` — +7 test methods in TestJailbreakExtraction (~54 LOC)
- `tests/test_jailbreak_patterns.py` — updated count assertion from 13→15 (1 line)
- `auto-improvement/research/2026-05-24T00-00_3-jailbreak-extraction.md` — new research file
- `auto-improvement/changes/2026-05-24T00-00_changes.md` — this file
- `auto-improvement/INDEX.md` — new row added
- `auto-improvement/ROTATION.md` — NEXT_INDEX advanced to 4
- `CHANGELOG.md` — two bullets added under ## [Unreleased] ### Hardened

---

## Quality Gate

- **Formatter:** `uv run ruff format aigis/ tests/` → 148 files left unchanged. ✓
- **Format check:** `uv run ruff format --check aigis/ tests/` → 148 files already formatted. ✓
- **Lint:** `uv run ruff check aigis/ tests/` → All checks passed. ✓
- **Tests:** 19 failed · 1633 passed · 5 skipped · 5 errors (measured via `uv run pytest --tb=no -q`)
  - Pre-existing failures: 14 in `tests/test_spec_lang.py` (YAML DSL), 2 in `tests/test_guard.py`,
    3 in `tests/test_oss_comparison_bench.py`, 5 errors in `tests/test_release_preflight.py`
    (environment-specific CI checks). None caused by this cycle's changes.
  - New tests added: 7 (all passing).

---

## Research File Used

`auto-improvement/research/2026-05-24T00-00_3-jailbreak-extraction.md`

---

## Implementation Caveats

- `jb_payload_splitting` (score 45) sits below the HIGH threshold by default. It needs a
  co-occurring signal to reach a block-level decision; this is intentional to limit false
  positives on legitimate numbered tutorials.
- `jb_chat_template_injection` (score 75) will produce very few false positives since these
  tokens are model-internal and have no legitimate use in user text.

---

## Pending Ideas

None new this cycle. Prior pending items `2026-05-09_conversation-history-injection.md`
(structural message-role validation) and `2026-05-13_control-plane-schema-jailbreak.md`
(JSON Schema enum jailbreaks) remain unimplemented due to LOC / infrastructure constraints.

---

## Release Decision Input

2 new detection rules added this cycle. The [Unreleased] section now has 2 rules total
(all from this cycle). Threshold for release is 3+ accumulated rules. **Not releasing this
cycle.** Accumulating for the next cycle.
