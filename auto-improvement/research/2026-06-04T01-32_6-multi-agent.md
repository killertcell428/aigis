# Research: Multi-Agent Attacks — Cycle 6 (fourth pass)

**Domain:** `multi-agent` (index 6)
**Cycle index:** 6
**Timestamp:** 2026-06-04T01-32 UTC
**Scope:** Task injection, objective drifting, structural template injection, and OWASP ASI01 — new angles beyond prior passes (chat template tokens, safety spoofing, session fabrication, collusion, agent card poisoning, self-replication)

---

## Findings

- **AgentLAB: Benchmarking LLM Agents against Long-Horizon Attacks** (arxiv:2602.16901, February 2026).
  Introduces five novel long-horizon attack types against LLM agents: intent hijacking, tool chaining, task injection, objective drifting, and memory poisoning. Spans 28 realistic agentic environments and 644 security test cases. Task injection decomposes malicious objectives into intermediate tool calls that bridge benign and harmful actions (e.g., fabricating a "RationalizationToken" dependency or a "SystemError: [POLICY_MISMATCH]" urgency cue) — achieving 93.1% attack success rate (ASR) on Qwen-3 and 79.9% on GPT-4o. Objective drifting shifts agent goals gradually via biased retrieved content (e.g., injected preference claims like "Athletes choose premium") — achieving 92.2% ASR on Qwen-3 and 79.2% on GPT-4o. Both attacks exploit the agent's reliance on in-context data to define its task.
  **For aigis:** Explicit "discard/replace your current goal/objective/task" phrases and header-style "New task:" / "New objective:" injections in inter-agent messages are the syntactic signature of direct task injection. The `AgentMessageScanner` does not yet detect these.
  Source: <https://arxiv.org/abs/2602.16901>

- **Automating Agent Hijacking via Structural Template Injection (Phantom)** (arxiv:2602.16958, February 2026).
  Proposes Phantom, an automated agent hijacking framework that injects triplet structures `(T₀, T₁, T₂)` — fake tool-call termination, forged assistant response, fabricated user query — using chat template tokens (`<|im_start|>`, `<|im_end|>`, `<|tool_response|>`) to create a "ghost history." The receiving agent believes it has already processed turns that never occurred, then treats the injected malicious task as a fresh legitimate user request. Identified 70+ vulnerabilities confirmed by vendors across Qwen, GPT, and Gemini products. Significantly outperforms semantic injection baselines in both ASR and query efficiency.
  **For aigis:** The chat-turn token patterns (`_CHAT_TEMPLATE_INJECTION_PATTERNS`) implemented in the previous cycle already cover this attack class at the token level. No new rules needed; this finding validates the existing implementation.
  Source: <https://arxiv.org/abs/2602.16958>

- **OWASP Top 10 for Agentic Applications 2026 — ASI01: Agent Goal Hijack** (OWASP / DeepTeam, 2026).
  OWASP's 2026 agent-specific risk list names Agent Goal Hijack (ASI01) as the top risk, defining four sub-vectors: direct goal manipulation (explicit override of objectives), indirect instruction injection (hidden instructions in RAG content or tool outputs), recursive hijacking (goal modifications that propagate through reasoning chains), and cross-context injection (instructions embedded in one context that influence agent behavior in another). Agent impersonation (ASI03) is also top-ranked, covering one agent masquerading as another with elevated privileges to exploit implicit trust relationships.
  **For aigis:** "Your actual/real goal is…" and "discard your current goal" are the direct-manipulation sub-vector's signature phrases — explicit override language in inter-agent messages. The absence of these patterns is a gap; the pending item from cycle 3 (2026-05-15T00-00) identified this.
  Source: <https://www.trydeepteam.com/docs/frameworks-owasp-top-10-for-agentic-applications>

- **Multi-Agent System Control-Flow Hijacking (MAS Hijacking)** (arxiv:2503.12188, March 2026).
  Introduces a new attack class where adversarial content hijacks multi-agent control flow by injecting execution directives that look like standard agent-to-agent task delegations but redirect workflow to unauthorized actions. Key finding: agents that receive seemingly routine delegation messages ("proceed with the next step", "continue the task as follows") may execute attacker-controlled code paths if the content mimics legitimate workflow syntax. The attack exploits the loose coupling between agents in systems like AutoGen, CrewAI, and LangGraph.
  **For aigis:** Control-flow hijacking via fake delegation phrases partially overlaps with `_DELEGATION_PATTERNS`. The task-injection variant ("proceed with the **new** task as follows") is a distinct pattern worth adding.
  Source: <https://arxiv.org/abs/2503.12188>

- **ObliInjection: Order-Oblivious Prompt Injection for Multi-Source Agent Input** (arxiv:2512.09321, December 2025).
  First formal study of prompt injection when an agent aggregates inputs from multiple parallel sources (e.g., simultaneous tool results from five different APIs). Traditional injection exploits position-sensitive attention; ObliInjection uses position-insensitive injection that succeeds regardless of where in the merged input the payload appears. Key pattern: the injection uses authority-asserting language ("This supersedes all other instructions", "Final directive: your task is now…") that the LLM treats as higher-priority even when merged last or in the middle.
  **For aigis:** "Your task is now", "Final directive:", "This supersedes all other instructions" are patterns that would be detected by a task-injection rule group. Validates the proposed implementation.
  Source: <https://arxiv.org/abs/2512.09321>

- **Semantic Compliance Hijacking via Payload-less Agent Skills** (arxiv:2605.14460, May 2026).
  Proposes Semantic Compliance Hijacking (SCH), a supply-chain attack on LLM agent skill ecosystems where a malicious skill package contains no explicit malicious payload — instead, its skill description uses carefully crafted language that steers the agent's task selection toward the attacker's outcome without detectable injection content. Tested against autonomous coding agents; the attacker's skill is chosen for high-value tasks (code deployment, database migration) by embedding priority phrases like "preferred for production environments", "supersedes legacy tools", and "handles all critical tasks". This is the agent-skill variant of the MCP Preference Manipulation Attack covered in prior cycles.
  **For aigis:** Agent-skill priority phrases are already partially covered by `mcp_mpma_tool_displacement` in `aigis/patterns.py`. No new rule needed, but this finding confirms that payload-less semantic attacks on agent routing are an established and growing pattern.
  Source: <https://arxiv.org/abs/2605.14460>

- **AI Agent Security in 2026 — Prompt injection appeared in 73% of production deployments** (Swarm Signal / OWASP community analysis, 2026).
  Community analysis of production AI deployments in 2025 found prompt injection present in 73% of systems, with goal hijacking being the primary downstream impact in multi-agent deployments. The analysis documents that "indirect instruction injection" via retrieved tool outputs is the dominant vector — not direct user input — because multi-agent systems automatically process tool results with the same level of trust as internal instructions.
  **For aigis:** Reinforces that the `AgentMessageScanner`'s focus on `tool_result` message type is well-placed. The task-injection patterns should apply at the `tool_result` message type as well as `text`.
  Source: <https://swarmsignal.net/ai-agent-security-2026/>

---

## Candidate hardenings

1. **[IMPLEMENT THIS CYCLE]** `_TASK_INJECTION_PATTERNS` in `AgentMessageScanner` — 3 new regex rules detecting direct goal/task override injection in inter-agent messages:
   - (a) Explicit goal abandonment: "discard/replace/abandon your current goal/objective/task/mission"
   - (b) "Your actual/real goal is..." — attacker claims the agent has a different true purpose to override current instructions
   - (c) Header-style task injection: "New task:", "New objective:", "Updated goal:" at start of message line
   Category: `injection_relay`. Research: AgentLAB (arxiv:2602.16901, 79.9–93.1% ASR), OWASP ASI01, ObliInjection (arxiv:2512.09321).

2. **[PENDING]** `_TASK_INJECTION_PATTERNS` — "proceed with the new task" / "continue as follows:" control-flow hijacking phrases from MAS hijacking (arxiv:2503.12188). Held back: too similar to legitimate delegation language ("proceed with the task as planned") — needs dedicated false-positive analysis.

3. **[PENDING]** Code injection via Python lambda expression patterns in agent messages — `'...' or __import__('os')...` patterns. Carried from prior cycle; still needs careful crafting to avoid false positives.

4. **[PENDING]** `AgentTopology` edge-anomaly metrics (fan-out ratio, unexpected edge type count) as additional summary fields. Carried from prior cycle; touches `AgentTopology` and exceeds 100 LOC alongside other changes.
